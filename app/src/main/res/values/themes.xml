<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PasswordGenerator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/sea_green</item>
        <item name="colorSecondary">@color/sea_green</item>
        <item name="android:colorMultiSelectHighlight">@color/blue</item>
    </style>

    <style name="Theme.PasswordGenerator" parent="Base.Theme.PasswordGenerator" />

</resources>